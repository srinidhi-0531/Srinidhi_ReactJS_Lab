// Define an interface for the data structure used in the application
interface IDataList  {
    payeeName: string,
    product: string,
    price: number,
    setDate: string,
    id: number
}

// Export the IDataList interface for use in other parts of the application
export default IDataList;